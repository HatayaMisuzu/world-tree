import { createDebugInfo, createWrapperResult } from "../wrapper-utils.js";
const ID="timeline.branch_tree"; export default Object.freeze({ id:ID,legacyId:null,name:"Timeline Branch Tree",status:"implemented",buildContext(ctx={}){return createWrapperResult(ID,null,{activeBranchId:ctx.activeBranchId||"main",branchCount:Number(ctx.branchCount||1),mergeSupported:false});},buildPromptBlock(ctx={}){const d=this.buildContext(ctx).data;return `【时间分支】active=${d.activeBranchId}; branches=${d.branchCount}; merge=false`;},getDebugInfo(ctx={}){return createDebugInfo(this,"src/core/timeline/branch-manager.js",this.buildContext(ctx),"branch-local");} });
