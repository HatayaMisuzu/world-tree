import { createDebugInfo, createWrapperResult } from "../wrapper-utils.js";
const ID="processing.completion_engine"; export default Object.freeze({ id:ID,legacyId:null,name:"Processing Completion Engine",status:"implemented",buildContext(ctx={}){return createWrapperResult(ID,null,{candidateCount:(ctx.processingCandidates||[]).length,canonicalWrites:0,sourceRequired:true});},buildPromptBlock(ctx={}){const d=this.buildContext(ctx).data;return `【素材处理】candidates=${d.candidateCount}; canonWrites=0`;},getDebugInfo(ctx={}){return createDebugInfo(this,"src/core/processing/processing-delivery.js",this.buildContext(ctx),"candidate-only");} });
