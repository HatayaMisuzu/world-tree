import { createProposal } from "../system/proposal-bus.js";
export function createCompletionProposal(candidate = {}) { return createProposal({ type: candidate.suggestedProposalType || "content_create", targetFile: candidate.suggestedTarget || "", summary: candidate.summary || candidate.title || "Material candidate", reason: `Material source: ${candidate.source?.label || "unknown"}`, impactLevel: candidate.riskLevel === "critical" ? "critical" : candidate.riskLevel === "high" ? "major" : "medium", changeCategory: candidate.type === "worldbookCandidate" ? "lore" : "description", affectedEntities: candidate.relatedEntities || [], patch: {} }); }
