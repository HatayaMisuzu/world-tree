export function extractMaterialCandidates(material = {}, content = "", options = {}) { const lines = String(content).split(/\r?\n/).map((line) => line.trim()).filter(Boolean).slice(0, 8); return lines.map((line, index) => ({ id: `cand_${material.id}_${index + 1}`, materialId: material.id, type: options.type || "worldbookCandidate", title: line.slice(0, 80), summary: line.slice(0, 240), suggestedTarget: "shared/worldbook.json", confidence: line.length > 30 ? "high" : "medium", riskLevel: options.riskLevel || "medium", conflicts: [], requiresApproval: true, suggestedProposalType: "worldbook_entry_create", source: { label: material.sourceLabel, hash: material.contentHash } })); }
