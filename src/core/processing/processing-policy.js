export function evaluateProcessingDelivery(candidate = {}, score = {}) { const reasons = []; if (candidate.riskLevel === "critical") reasons.push("critical_risk"); if (score.consistency === "low") reasons.push("low_consistency"); if (candidate.conflicts?.length) reasons.push("unresolved_conflict"); if (candidate.source?.label === "unknown" && candidate.riskLevel !== "low") reasons.push("unknown_source"); return { allowed: reasons.length === 0, reasons, requiresApproval: true }; }
