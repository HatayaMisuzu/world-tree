const CONTINUE_PATTERNS = [/^continue\b/i, /^proceed\b/i, /what happens next/i, /^then\??$/i, /继续/, /推进一下/, /看看接下来/, /然后呢/];
export function isContinueIntent(input = "") { const text = String(input).trim(); return CONTINUE_PATTERNS.some((pattern) => pattern.test(text)); }
export function canAutoLight(input = {}) { const reasons = []; if (!isContinueIntent(input.userInput)) reasons.push("no_continue_intent"); if (["quick-setting", "creation-forge"].includes(input.modeId)) reasons.push("mode_blocks_auto_light"); if (!input.profile?.autoAdvance?.allowAutoLight) reasons.push("profile_blocks_auto_light"); if (input.advanceMode !== "auto-light") reasons.push("mode_not_auto_light"); if (input.directorPlan?.shouldAdvanceScene === false && input.directorPlan?.beatType === "choice_point") reasons.push("director_blocks_advance"); if (Object.values(input.telemetry?.metrics || {}).includes("critical")) reasons.push("telemetry_critical"); if (input.activeProposals?.some((item) => item.status === "pending" && item.impactLevel === "critical")) reasons.push("critical_proposal_pending"); if (input.hiddenTruthRequired) reasons.push("hidden_truth_required"); return { allowed: reasons.length === 0, reasons }; }
