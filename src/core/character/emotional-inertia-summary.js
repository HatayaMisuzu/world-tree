export function summarizeCharacterInertia(data = {}, presentCharacterIds = []) { const ids = presentCharacterIds.length ? presentCharacterIds : Object.keys(data.characters || {}); return ids.slice(0, 6).filter((id) => data.characters?.[id]).map((id) => ({ characterId: id, toward: data.characters[id].toward, address: data.characters[id].address, distance: data.characters[id].distance, trust: data.characters[id].trust, tension: data.characters[id].tension, secret: data.characters[id].secret, warnings: [] })); }
