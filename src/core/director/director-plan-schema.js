export const SAFE_DIRECTOR_PLAN = Object.freeze({ beatType: "respond", pace: "normal", tension: "steady", focus: ["user_intent", "current_scene"], shouldAdvanceScene: false, shouldRevealSecret: false, stopAtChoicePoint: true, maxNewEvents: 0, forbiddenMoves: [], reason: "Fallback safe director plan." });
export function normalizeDirectorPlan(plan = {}) { return { ...SAFE_DIRECTOR_PLAN, ...plan, focus: Array.isArray(plan.focus) ? plan.focus.slice(0, 3) : [...SAFE_DIRECTOR_PLAN.focus], forbiddenMoves: Array.isArray(plan.forbiddenMoves) ? plan.forbiddenMoves.slice(0, 12) : [], maxNewEvents: Math.max(0, Math.min(1, Number(plan.maxNewEvents ?? 0))) }; }
