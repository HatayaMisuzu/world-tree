# World Tree Desktop LLM 输出协议

先输出玩家可读叙事正文。需要持久化状态时，在正文后追加中文标记段。

支持段：

- `【状态】`
- `【角色】`
- `【正史】`
- `【世界书提案】`
- `【记忆】`
- `【场景预测】`

段内使用 YAML 风格键值对，不使用 JSON，不依赖工具调用。

解析失败不阻塞叙事；Desktop 会维持上轮状态并在下一轮提示修正。
