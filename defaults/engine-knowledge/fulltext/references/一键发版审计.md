# 一键发版审计

> world-tree v12.19 创新升级模块。定位：把发版前自检收束成一个可执行入口。

## 命令

```powershell
python C:\Users\Lenovo\AppData\Local\hermes\skills\creative\world-tree\scripts\world_tree_release_audit.py
```

## 审计内容

1. 运行 `scripts/world_tree_contract_check.py`，要求 `fail=0`。
2. 分别校验 shared 与 writer 的 world-tree skill。
3. 解析 `D:/world-tree-data/_engine/canon_state.template.json`。
4. 解析 `D:/world-tree-data/_engine/agent_profiles.json`。
5. 输出 `RELEASE_AUDIT PASS` 或列出阻断项。

## 放行规则

- 任一检查失败即不得交付。
- 失败时先修复当前生效文件，再同步 writer 副本。
- 修复后必须重新运行本脚本。
